{{> head}}
{{> navbar}}
{{#page}}
   <main class="post">
      <div class="wrapper">
         <div class="content">
            <article>
               {{#featuredImage}}
                  {{#if url}}
                     <figure
                        class="content__hero-image content__hero-image--nomargin content__hero-image--full">
                        <img
                           src="{{url}}"
                           {{#if @config.site.responsiveImages}}                           
                              {{responsiveImageAttributes 'featuredImage' srcset.post sizes.post}}
                           {{/if}}
                           {{ lazyload "eager" }}
                           height="{{height}}"
                           width="{{width}}"
                           alt="{{alt}}">
                        {{#checkIfAny caption credits}}
                           <figcaption>
                              {{caption}}
                              {{credits}}
                           </figcaption>
                        {{/checkIfAny}}
                     </figure>  
                  {{/if}}
               {{/featuredImage}}
               <header class="content__inner content__header content__header--full">
                  <h1 class="content__title">{{title}}</h1>

                  {{#checkIfAny @config.page.displayAuthor @config.page.displayDate @config.page.displayComments}}
                     <div class="card__meta content__meta">
                        {{#if @config.page.displayAuthor}}
                           <div class="content__author">{{ translate 'post.publishedBy' }}
                              {{#author}}
                                 <a href="{{url}}" class="invert" rel="author">
                                    {{name}}</a>
                              {{/author}}
                           </div>
                        {{/if}}

                        {{#if @config.page.displayDate}}
                           <time datetime="{{date createdAt 'YYYY-MM-DDTHH:mm'}}">
                              {{#checkIf @config.custom.formatDate '!=' 'custom'}}
                                 {{date createdAt @config.custom.formatDate}}
                              {{else}}
                                 {{date createdAt @config.custom.formatDateCustom}}
                              {{/checkIf}}
                           </time>
                        {{/if}}
                         
                        {{#if @config.page.displayComments}}
                           <div class="card__comments">
                               <a
                                 href="{{url}}#post-comments"
                                 class="invert"
                                 rel="nofollow" 
                                 aria-label="{{ translate 'post.comments' }}">                          
                                 <svg aria-hidden="true">
                                    <use xlink:href="{{@website.assetsUrl}}/svg/svg-map.svg#comments"/>
                                 </svg>
                              </a>
                           </div>
                        {{/if}}
                         
                     </div>
                  {{/checkIfAny}}
               </header>
                
               {{#if @config.custom.postAd1}}
                  <div class="ads">
                     <div class="content__inner">
                        {{{@config.custom.postAd1}}}
                     </div>
                  </div>
               {{/if}}

               <div class="content__inner content__entry">
                  {{{text}}}
               </div>

               {{#checkIfAny @config.page.displayUpdatedDate @config.page.displayShareButtons @config.page.displayAuthorBio @config.page.displayChildPages @config.page.displayComments}}
                  <footer class="content__inner  content__footer">

                     {{#if @config.page.displayUpdatedDate}}
                        <div class="content__updated">{{ translate 'post.lastUpdatedDate' }}
                           <time datetime="{{date modifiedAt 'YYYY-MM-DDTHH:mm'}}">
                              {{#checkIf @config.custom.formatDate '!=' 'custom'}}
                                 {{date modifiedAt @config.custom.formatDate}}
                              {{else}}
                                 {{date modifiedAt @config.custom.formatDateCustom}}
                              {{/checkIf}}
                           </time>
                        </div>
                     {{/if}}

                     {{#if @config.page.displayShareButtons}}
                        {{#checkIfAll @plugins.socialSharing @plugins.socialSharing.state}}
                           <div class="content__share">
                              <h3 class="content__footer-title">{{ translate 'post.shareStory' }}</h3>
                              <div class="content__share-inner">
                                 {{{@customSocialSharing}}}
                              </div>
                           </div>
                        {{else}}
                           <div class="content__share">
                              <h3 class="content__footer-title">{{ translate 'post.shareStory' }}</h3>
                              <div class="content__share-inner">
                                 {{> share-buttons}}
                              </div>
                           </div>
                        {{/checkIfAll}}          
                     {{/if}}

                     {{#if @config.page.displayAuthorBio}}
                        <div class="author">
                           {{#author}}
                              {{#if avatar}}
                                 <img  src="{{avatarImage.url}}" 
                                       class="author__avatar" 
                                       {{ lazyload "lazy" }}
                                       alt="{{avatarImage.alt}}" 
                                       height="{{avatarImage.height}}" 
                                       width="{{avatarImage.width}}">                             
                              {{/if}}
                              <div class="author__info">
                                 <h3 class="h5  author__name">
                                    <a href="{{url}}" class="invert" rel="author">
                                       {{name}}
                                    </a>
                                 </h3>
                                 {{{description}}}
                              </div>
                           {{/author}}
                        </div>
                     {{/if}}

                     {{#if @config.page.displayChildPages}}
                        {{#if subpages}}
                           <div class="subpages">
                              <h2 class="h5 subpages__title">{{ translate 'page.childPages' }}</h2>
                              <ul class="subpages__list">
                                 {{> subpages-list}}
                              </ul>
                           </div>
                        {{/if}}
                     {{/if}}

                     {{#if @config.page.displayComments}}
                        <div class="content__comments" id="post-comments">
                           {{#checkIf @config.custom.commentOnClick '==' "click" }}
                              <button class="content__comments-button js-content__comments-button">{{ translate 'post.loadComments' }}</button>
                              <div
                                 class="content__comments-inner content__comments-inner--hidden js-content__comments-inner">
                                 {{{@commentsCustomCode}}}
                              </div>
                           {{else}}
                              <div class="content__comments-inner">
                                {{{@commentsCustomCode}}}
                              </div>
                           {{/checkIf}}
                        </div>
                     {{/if}}
                      
                     {{#if @config.page.displayShareButtons}}
                        <div class="content__bar contentbar" id="js-content__bar">
                           <div class="contentbar__inner contentbar__inner--center">

                                 {{#checkIfAll @plugins.socialSharing @plugins.socialSharing.state}}
                                    <div class="contentbar__share">
                                       <div class="contentbar__share-inner">
                                          <h3>{{ translate 'post.share' }}</h3>
                                          {{{@customSocialSharing}}}
                                       </div>
                                    </div>
                                 {{else}}
                                    <div class="contentbar__share">
                                       <div class="contentbar__share-inner">
                                          <h3>{{ translate 'post.share' }}</h3>
                                          {{> share-buttons-contentbar}}
                                       </div>
                                    </div>
                                 {{/checkIfAll}}           

                              <div class="contentbar__progress" id="js-contentbar__progress" aria-hidden="true"></div>

                           </div>
                        </div>
                     {{/if}}

                  </footer>
               {{/checkIfAny}}
            </article>
         </div>
      </div>
       
      {{#if @config.custom.postAd2}}
         <div class="ads ads--mb">
            <div class="wrapper">
               {{{@config.custom.postAd2}}}
            </div>
         </div>
      {{/if}}

   </main>
{{/page}}
{{> footer}}
